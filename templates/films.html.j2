{% extends "layout.html.j2" %} {% block title %}Film Flock | Discover{% endblock
%} {% block body %}
<div class="min-vh-100 py-4">
  <div class="container">
    <h1>Discover</h1>
    <div>
      <label for="search"></label>
      <input
        type="text"
        name="search"
        id="search"
        placeholder="Search Collections"
      />
    </div>
    <div class="container p-0">
      {% for movie in movies %}
      <div>
        <h2>{{movie.title}}</h2>
      </div>
      {% endfor %}
    </div>
  </div>
</div>

{% endblock %}
